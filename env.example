# Django Settings
# ==============

# Secret key for Django - CHANGE THIS IN PRODUCTION!
# Generate a new secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-change-me-in-production

# Debug mode - set to False in production
DEBUG=True

# Allowed hosts (comma-separated list)
# For production, add your domain: ALLOWED_HOSTS=example.com,www.example.com
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# ======================
# For SQLite (default, no setup needed):
DATABASE_URL=sqlite:///db.sqlite3

# For PostgreSQL (example):
# DATABASE_URL=postgres://user:password@localhost:5432/email_mailer

# For MySQL (example):
# DATABASE_URL=mysql://user:password@localhost:3306/email_mailer

# Email Configuration
# ===================
# SMTP server settings for sending emails

# Gmail example:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Other email providers:
# For Outlook/Office365:
# EMAIL_HOST=smtp.office365.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True

# For SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-api-key

# For Mailgun:
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_USE_TLS=True

# Ollama AI Settings
# ==================
# URL for Ollama API (default is localhost)
OLLAMA_API_URL=http://localhost:11434/api/generate

# Ollama model to use for rewriting emails
# Common models: llama2, mistral, codellama, granite4:1b, etc.
# List available models: ollama list
OLLAMA_MODEL=granite4:1b

# Notes:
# ======
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values above with your actual configuration
# 3. Never commit .env to version control (it should be in .gitignore)
# 4. For Gmail, you'll need to use an "App Password" instead of your regular password
#    - Go to Google Account > Security > 2-Step Verification > App passwords
# 5. Make sure Ollama is running: ollama serve
# 6. Pull the model you want to use: ollama pull granite4:1b
